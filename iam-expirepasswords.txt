Expire passwords
  description "Password Policy is set to never expire passwords"
  suggested_action "Set the policy as such that users must create new passwords"


coreo_aws_advisor_alert "expirepasswords" do
  action :define
  service :iam
  description "Password Policy is set to never expire passwords"
  category "warning"
  suggested_action "Set the policy as such that users must create new passwords"
  level "medium"
  objectives ["account_password_policy"]
  audit_objects ["object.password_policy.expire_passwords"]
  operators ["=="]
  alert_when ["false"]
end
