Password reuse prevention
  description "Password Policy is set to allow the reuse of passwords"
  suggested_action "Set the policy as such that users cannot reuse passwords"



coreo_aws_advisor_alert "passwordreuseprevention" do
  action :define
  service :iam
  description "Password Policy is set to allow the reuse of passwords"
  category "danger"
  suggested_action "Set the policy as such that users cannot reuse passwords"
  level "medium"
  objectives ["account_password_policy"]
  audit_objects ["object.password_policy.password_reuse_prevention"]
  operators ["=="]
  alert_when [nil]
end
