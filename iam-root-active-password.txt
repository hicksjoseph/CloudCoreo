Root active password
  description "Root user has been logging in with a password"
  suggested_action "Re-set your root account password and lock it away in a safe"

coreo_aws_advisor_alert "root-active-password" do
  action :define
  service :iam
  description "Root user has been logging in with a password"
  category "warning"
  suggested_action "Re-set your root account password and lock it away in a safe"
  level "medium"
  id_map "object.user"
  objectives ["credential_report"]
  formulas ["CSV[user=<root_account>]"]
  audit_objects ["object.content.password_last_used"]
  operators [">"]
  alert_when ["15.days.ago"]
end
